import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shadow7.attendencerecordapp',
  appName: 'attendence-record-app',
  webDir: 'dist/public'
};

export default config;
